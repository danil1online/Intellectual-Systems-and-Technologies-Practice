# Практическая работа №1: Основы Git и GitLab

> 📖 Перед началом работы ознакомьтесь со [справочником по Markdown](./MD_Instructions.md) — он пригодится для оформления отчётов.
>
> 📌 **Для запуска автоматической оценки работы выполните финальное действие (описано ниже).**

---

## 🎯 Цель работы

Изучение распределённого контроля версий, концепции Git с понятиями репозитория и ветвления версий, порядка использования GitLab и его базовых операций.

---

## 📌 Задачи

- Развитие практических навыков работы с Git и GitLab: форк, клон, ветвление, merge, pull, push
- Практика работы с командами Git для совместной разработки
- Создание отчётов в формате Markdown с использованием ИИ-ментора

---

## 📚 Теоретические основы

### История Git

Разработка Linux в начале 2000-х осуществлялась в рамках системы BitKeeper. В 2005 году BitKeeper стал платным, и Линус Торвальдс создал новую систему контроля версий. Ключевые характеристики:

- поддержка нелинейного развития
- распределённая разработка: каждый разработчик имеет локальную копию полной истории
- совместимость с существующими системами и протоколами
- эффективное управление крупными проектами
- аутентификация истории: гарантирует идентичные обновления кода
- подключаемые стратегии слияния

Распределённые системы контроля версий (DVCS) стали важнейшими инструментами для совместной разработки ПО. Git — одна из самых популярных.

### Что такое Git?

Git — бесплатная распределённая система контроля версий под лицензией GNU GPL. Каждый разработчик получает полную копию репозитория на своём компьютере. Внесённые изменения можно синхронизировать с удалённым сервером.

### GitLab

GitLab — платформа DevOps, предоставляющая доступ к репозиториям Git, систему управления исходным кодом. Разработчики могут:

- Просматривать код, оставлять комментарии
- Работать со своей локальной копией кода
- Осуществлять ветвления и слияния
- Использовать CI/CD для тестирования и доставки

Другие платформы: GitHub, BitBucket.

### Основные термины

- *Протокол SSH* — метод безопасного удалённого входа.
- *Репозиторий* — место, содержащее папки проекта с контролем версий.
- *Форк / fork* — копия репозитория на сервере GitLab.
- *Merge request (MR)* — запрос на рассмотрение и утверждение изменений.
- *Working directory* — рабочая директория, связанная с Git-репозиторием.

### Основные команды Git

- *git add* — перемещает изменения из рабочего каталога в staging area.
- *git status* — состояние рабочего каталога и снимок изменений.
- *git commit* — снимок изменений и фиксация в проекте.
- *git reset* — отмена изменений в файлах.
- *git log* — предыдущие изменения в проекте.
- *git branch* — создание изолированной среды для изменений.
- *git checkout* — просмотр и изменение веток.
- *git merge* — соединение веток.
- *git push* — отправка изменений на сервер.
- *git pull* — получение обновлений с сервера.

### Ветвление (Branch)

В Git файлы хранятся в ветках. Основная ветка (main) — развертываемая версия кода. При планировании изменений создаётся новая ветка как точная копия исходной. Изменения в дочерней ветке не попадают автоматически в main.

### Коммит (Commit)

Фиксация изменений. Разработчик должен написать содержательный комментарий:

- Не заканчивайте сообщение точкой.
- Не более 50 символов — для подробностей используйте расширенное описание.

### Merge Request

Изменения в ветке публикуются только после утверждения:

1. Создаётся merge request.
2. Код рассматривается и утверждается.
3. Ветка сливается с main.

### Fork и Clone

**Clone** — локальная копия удалённого репозитория:

```bash
git clone git@gitlab.example.com:user/repo.git
```

**Fork** — полная копия репозитория на GitLab-сервере. Проект-родитель называется *upstream*.

---

## 📁 Материалы и методы

- Операционная система — [Ubuntu 22.04](https://help.ubuntu.ru/wiki/командная_строка)
- Язык программирования — [bash](https://habr.com/ru/articles/47163/)
- Основные технологии:
  - [git](https://habr.com/ru/articles/541258/)

---

## 🧪 Программа работы

### ⚙️ Настройка среды

**Авторизоваться на сервере [JupyterHub](https://jupyter.org/hub) по адресу `http://<server-ip>:8000/`**

> ⚠️ Замените `<server-ip>` на IP-адрес вашего сервера (указан преподавателем).

1. Откройте браузер и перейдите по адресу `http://<server-ip>:8000/`
2. Нажмите **Keycloak** для входа
3. Войдите под своей учётной записью (`student_<группа>_<номер>`)

![Авторизация](https://github.com/danil1online/intelligent_information_and_measurement_systems/blob/main/images/autorization.png)

**Создать новую вкладку Terminal**

1. Нажмите символ **+** в верхней части JupyterLab
2. Выберите **Terminal**

![Создание вкладки Terminal](https://github.com/danil1online/intelligent_information_and_measurement_systems/blob/main/images/terminal_window_create.png)

---

### 📁 Форк проекта

1. Откройте **GitLab**: `http://<server-ip>:80`
2. Войдите через **Keycloak**
3. Перейдите в проект **students/project**
4. Нажмите **Fork** → **Create fork**
5. Ваш проект появится по адресу: `http://<server-ip>:80/<username>/project`

---

### 📁 Клонирование и работа через командную строку

**Клонирование вашего форкнутого репозитория:**

```bash
git clone git@gitlab.<server-ip>:<username>/project.git
cd project
```

> ⚠️ Для работы по SSH убедитесь, что ваш SSH-ключ добавлен в GitLab (см. Практическую работу №0).

**Настройка пользователя Git:**

```bash
git config --global user.email "student_<группа>_<номер>@academic"
git config --global user.name "Иванов Иван"
```

**Проверка статуса и история:**

```bash
git status
git log
```

> 💡 Для выхода из `git log` нажмите `q`.

**Создание и переключение ветки:**

```bash
git branch feature-1
git checkout feature-1
```

> 💡 Можно создать и сразу перейти в ветку:
> ```bash
> git checkout -b feature-1
> ```

**Создание файла, добавление и фиксация:**

```bash
echo "# Practice 1" > test.md
git add test.md
git commit -m "Add test.md"
```

**Откат изменений:**

```bash
git revert HEAD --no-edit
```

**Слияние ветки с main:**

```bash
git checkout main
git merge feature-1
git log
```

**Отправка изменений на сервер:**

```bash
git push origin main
```

**Получение обновлений:**

```bash
git pull origin main
```

**Удаление объединённой ветки:**

```bash
git branch -d feature-1
```

---

## 📌 Задание

Выполните в терминале JupyterLab:

1. Откройте терминал и перейдите в клонированный репозиторий
2. Создайте ветку `newbranch`
3. Переключитесь на неё
4. Создайте файл `newbranchfile` с любым содержимым
5. Добавьте файл и зафиксируйте изменения
6. Отмените последний коммит (`git revert HEAD --no-edit`)
7. Создайте файл `newgoodfile`
8. Добавьте и зафиксируйте его
9. Переключитесь на `main`
10. Объедините `newbranch` с `main`
11. Отправьте изменения на сервер (`git push`)
12. Удалите ветку `newbranch`
13. Проверьте `git log` — история должна содержать все ваши коммиты

**Результат:** вывод `git log`, показывающий ветку, merge и коммиты.

---

## 📝 LLM_Help.ipynb

Создайте ноутбук `LLM_Help.ipynb` в папке `reports/practice1` и задайте уточняющие вопросы к ИИ-ментору по теме работы через `%%ask_mentor`. Примеры:

```
%%ask_mentor
Вот мой код: git checkout -b feature-1
git commit -m "test"
Но git log не показывает новый коммит. Почему?
```

---

## 📌 Отчёт

1. Опишите выполненную работу
2. Приложите вывод `git log`
3. Сохраните отчёт как `Pr_1_<группа>_<номер>.md` в `reports/`
4. Загрузите в репозиторий и отправьте:

```bash
cp Pr_1_<группа>_<номер>.md reports/
git add .
git commit -m "Pr_1 report"
git push
```

> 📖 Используйте [справочник по Markdown](./MD_Instructions.md) для оформления.

---

**Финальное действие:**

Для запуска автоматической оценки создайте триггер-файл и отправьте его в репозиторий:

```bash
touch .grade-trigger
git add .grade-trigger
git commit -m "Grade trigger"
git push
```

> ⏳ Оценка запустится автоматически в GitLab CI. Результат появится в разделе **CI/CD → Pipelines**.

---

## Критерии оценки

| Критерий | Баллы |
|----------|-------|
| Теория: определение DVCS, Git, GitLab, веток, коммитов | 2 |
| Задание: все команды выполнены верно | 3 |
| LLM_Help.ipynb с вопросами к ментору | 2 |
| Отчёт оформлен в Markdown | 1 |
| Файлы загружены в репозиторий | 1 |
| **Итого** | **9** |
