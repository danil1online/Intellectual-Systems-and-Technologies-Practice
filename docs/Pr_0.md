# Практическая работа №0: Предварительная настройка учебного комплекса

## Цель работы
Настроить рабочее окружение: регистрация в Keycloak, настройка SSH-ключей, первый вход в JupyterHub и Nextcloud.

---

## 1. Регистрация в Keycloak

1. Откройте в браузере **GitLab**: `http://<server-ip>:80`
2. Нажмите **Sign in** → **Keycloak** (кнопка OIDC входа)
3. На странице Keycloak нажмите **Register** (или перейдите напрямую:
   `http://<server-ip>:9200/auth/realms/istp/login-actions/registration`)
4. Заполните форму:
   - **Username**: `student_<группа>_<номер>` (например: `student_pia_01`)
   - **Email**: ваш студенческий email
   - **Password**: придумайте надёжный пароль (запомните его!)
5. Нажмите **Register**

> ✅ Ваша учётная запись создана в Keycloak. С этими же данными вы можете войти в GitLab, JupyterHub и Nextcloud.

---

## 1.1. Учётные записи преподавателей

Для лектора и лаборанта заранее созданы учётные записи:
- **lecturer_01** / пароль из `.env` → `LECTURER_01_PASSWORD`
- **lecturer_02** / пароль из `.env` → `LECTURER_02_PASSWORD`

> ⚠️ **Обязательно смените пароли после первого входа!**

---

## 2. Доступы к сервисам

| Сервис | URL | Логин | Пароль |
|--------|-----|-------|--------|
| **GitLab** | `http://<server-ip>:80` | через Keycloak | тот же |
| **JupyterHub** | `http://<server-ip>:8000` | через Keycloak | тот же |
| **Nextcloud** | `http://<server-ip>:8080` | через Keycloak | тот же |
| **Dashboard** | `http://<server-ip>:9000` | только преподаватель | — |

---

## 3. Генерация SSH-ключа

Студентам необходимо сгенерировать SSH-ключ для работы с GitLab из терминала JupyterLab.

### Шаг 1: Откройте JupyterHub и Terminal

1. Откройте **JupyterHub**: `http://<server-ip>:8000`
2. Войдите через **Keycloak** (кнопка "Keycloak" на странице входа)
3. В главном меню JupyterLab нажмите **File → New → Terminal**

**Важно:** параллельно откройте **GitLab** в другой вкладке браузера — вы будете копировать туда SSH-ключ.

### Шаг 2: Сгенерируйте SSH-ключ

```bash
ssh-keygen -t ed25519 -C "student_<группа>_<номер>@academic"
```

Нажмите Enter на все вопросы (без passphrase).

> 💡 SSH-ключ генерируется автоматически при первом входе. Если ключ уже создан, пропустите этот шаг.

### Шаг 3: Посмотрите публичный ключ

```bash
cat ~/.ssh/id_ed25519.pub
```

Вы увидите строку вида:
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... student_pia_01@academic
```

**Скопируйте эту строку!**

### Шаг 4: Добавьте ключ в GitLab

1. Откройте **GitLab**: `http://<server-ip>:80`
2. Войдите под своим логином
3. Перейдите: **Settings (меню слева) → SSH Keys**
4. Вставьте скопированный ключ в поле **Key**
5. Нажмите **Add key**

> 🔑 Ключ добавлен! Теперь вы можете работать с Git по SSH.

---

## 4. Создание репозитория для отчётов

1. В GitLab нажмите **New Project** → **Create blank project**
2. Название: `reports_<группа>_<номер>`
3. Visibility: **Private**
4. Нажмите **Create project**

Этот репозиторий вы будете использовать для загрузки всех отчётов по практическим работам.

---

## 5. Доступ к методическим материалам

Все методические материалы (`.md` файлы) доступны в репозитории GitLab:

**Способ 1: Через веб-интерфейс GitLab**
1. Откройте **GitLab**: `http://<server-ip>:80`
2. Найдите репозиторий `methodical_docs` (в группе `methodical` или в вашем профиле)
3. Откройте любой файл `.md` — он отобразится в отформатированном виде

**Способ 2: Через терминал JupyterLab**
```bash
git clone http://<server-ip>/methodical/methodical_docs.git
cd methodical_docs
ls -la
```

**Способ 3: В Nextcloud**
1. Откройте **Nextcloud**: `http://<server-ip>:8080`
2. Используйте встроенный редактор Markdown для создания файлов

---

## 6. Nextcloud для хранения отчётов

1. Откройте **Nextcloud**: `http://<server-ip>:8080`
2. Войдите через **Keycloak**
3. Создайте папку `reports_<группа>_<номер>`
4. В Nextcloud можно создавать файлы `.md` нативно — без сторонних приложений
5. Используйте справочник [Синтаксис Markdown](./MD_Instructions.md) для форматирования отчётов

---

## 7. Загрузка отчёта

1. В терминале JupyterLab клонируйте ваш репозиторий отчётов:
```bash
git clone git@gitlab.<server-ip>:<username>/reports_<группа>_<номер>.git
cd reports_<группа>_<номер>
```
2. Сохраните отчёт как `Pr_0_<группа>_<номер>.md`
3. Скопируйте в репозиторий и отправьте:
```bash
cp Pr_0_<группа>_<номер>.md .
git add .
git commit -m "Pr_0 report"
git push
```

---

## Отчёт по работе

1. Сделайте скриншоты:
    - Ваша учётная запись в GitLab (Settings → Account)
    - Ваш SSH-ключ в GitLab (Settings → SSH Keys)
    - Терминал JupyterLab с результатом `cat ~/.ssh/id_ed25519.pub`
2. Сохраните отчёт как `Pr_0_<группа>_<номер>.md` в формате Markdown
3. Загрузите файл `.md` в репозиторий **reports** в GitLab

---

## Критерии оценки

| Критерий | Баллы |
|----------|-------|
| Учётная запись в Keycloak создана | 2 |
| SSH-ключ сгенерирован и добавлен | 3 |
| Скриншоты выполнены корректно | 2 |
| Репозиторий отчётов создан | 2 |
| Отчёт загружен в репозиторий | 1 |
| **Итого** | **10** |
