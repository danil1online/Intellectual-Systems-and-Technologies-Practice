#!/usr/bin/env python3
"""Per-practice grading specs for ISTP LLM grading.

Каждая spec заменяет промпт «вся методичка + общий критерий» на компактный
пункт-в-пункт чек-лист конкретной практики:

- tasks:     пронумерованные шаги задания; у каждого шага text (описание для
             промпта) и markers (регулярки команд-критериев для grounding);
- questions: контрольные вопросы с якорями-концептами верного ответа;
- *_rules:   правила доказательности (что засчитывается, что нет).

GROUNDING (анти-галлюцинация): LLM выдает только компактные булевы массивы,
а код проверяет их независимо:

    task[i]     = LLM_done[i] AND любой marker шага найден в тексте задания отчёта
    question[j] = LLM_correct[j] AND в блоке ответов есть пункт j с содержимым
                  сверх самого вопроса AND в ответе >= min_keywords ключевых
                  слов концепта (spec questions[i]['keywords'])

Баллы считает код (normalize_spec_payload):
    task_score     = TASK_TOTAL * выполнено / всего шагов        (0..3)
    question_score = QUESTION_TOTAL * верно / всего вопросов     (0..2)
    final          = round(task_score + question_score)          (0..5)

Формат ответа LLM (строго один JSON-объект):
    {"tasks": [true/false ... N], "questions": [true/false ... M],
     "feedback": "...", "issues": ["..."], "recommendations": ["..."]}
"""

PRACTICE_SPECS = {
    1: {
        "title": "Основы Git и GitLab",
        "tasks": [
            {
                "text": "Терминал открыт в корневом каталоге репозитория `project` — в отчёте виден путь `project` в промпте терминала или команда `cd` в этот каталог",
                "markers": [r"cd\s+project", r"project\$"],
            },
            {
                "text": "Создана ветка `newbranch` — команда `git branch newbranch` (эквивалент: `git checkout -b newbranch`)",
                "markers": [r"git\s+branch\s+newbranch\b", r"git\s+checkout\s+-b\s+newbranch\b"],
            },
            {
                "text": "Переключение на ветку `newbranch` — команда `git checkout newbranch` (если шаг 2 выполнен как `git checkout -b newbranch`, этот шаг считается выполненным вместе с ним)",
                "markers": [r"git\s+checkout\s+newbranch\b", r"git\s+switch\s+newbranch\b", r"git\s+checkout\s+-b\s+newbranch\b"],
            },
            {
                "text": "Создан файл `newbranchfile` с любым содержимым — `echo \"...\" > newbranchfile`, `touch newbranchfile` или создание через редактор",
                "markers": [r"newbranchfile"],
            },
            {
                "text": "Файл `newbranchfile` добавлен и зафиксирован — `git add newbranchfile` и `git commit -m \"...\"`",
                "markers": [r"git\s+add\s+newbranchfile", r"git\s+add\s+\.", r"git\s+add\s+-A", r"git\s+add\s+\*"],
            },
            {
                "text": "Последний коммит отменён — `git revert HEAD --no-edit` (эквивалент: `git revert HEAD`)",
                "markers": [r"git\s+revert\s+HEAD"],
            },
            {
                "text": "Создан файл `newgoodfile` — `echo \"...\" > newgoodfile`, `touch newgoodfile` или создание через редактор",
                "markers": [r"newgoodfile"],
            },
            {
                "text": "Файл `newgoodfile` добавлен и зафиксирован — `git add newgoodfile` и `git commit -m \"...\"`",
                "markers": [r"git\s+add\s+newgoodfile", r"git\s+add\s+\.", r"git\s+add\s+-A", r"git\s+add\s+\*"],
            },
            {
                "text": "Переключение на `main` — команда `git checkout main`",
                "markers": [r"git\s+checkout\s+main\b", r"git\s+switch\s+main\b"],
            },
            {
                "text": "Слияние ветки с `main` — команда `git merge newbranch`",
                "markers": [r"git\s+merge\s+newbranch\b"],
            },
            {
                "text": "Отправка изменений на сервер — `git push` (эквивалент: `git push origin main`)",
                "markers": [r"git\s+push\b"],
            },
            {
                "text": "Удаление ветки `newbranch` — `git branch -d newbranch` (эквивалент: `git branch -D newbranch`)",
                "markers": [r"git\s+branch\s+-[dD]\s+newbranch\b"],
            },
            {
                "text": "Проверка истории — в отчёте есть вывод `git log`, в котором видны коммиты (включая Revert и Merge)",
                "markers": [r"git\s+log\b"],
            },
        ],
        "task_rules": [
            "Шаг считается выполненным (true) ТОЛЬКО если в тексте отчёта есть соответствующая команда (в блоке кода или в тексте) и/или её вывод в терминале.",
            "Фраза «выполнил», «создал ветку», «закоммитил» БЕЗ самой команды — это НЕ выполнение (false).",
            "Указанные в шаге эквивалентные команды засчитываются как исходные.",
            "Не выдумывай выполнение: если команды нет в тексте отчёта — шаг не выполнен, даже если результат упоминается.",
        ],
        "questions": [
            {
                "text": "Зачем в Git нужны ветки и чем ветка отличается от коммита?",
                "anchors": [
                    "ветка — отдельная линия разработки / изолированная среда: изменения в ветке не влияют на main до слияния",
                    "коммит — снимок (фиксированное состояние) файлов в определённый момент истории",
                ],
                "keywords": ["лини", "разработк", "снимок", "состояни", "истори", "отдельн", "изолир", "точк"],
            },
            {
                "text": "Как связываются `git add`, `git commit` и `git push` при отправке изменений в репозиторий?",
                "anchors": [
                    "git add — перенос изменений в staging area (индекс)",
                    "git commit — фиксация снимка локально (коммит в локальной истории)",
                    "git push — отправка коммитов в удалённый репозиторий (на сервер)",
                    "порядок: сначала add, затем commit, затем push",
                ],
                "keywords": ["staging", "индекс", "локальн", "удалённ", "сервер", "снимок", "порядок", "сначала", "затем", "фиксиру"],
            },
            {
                "text": "Чем `git merge` отличается от `git revert` и когда применяется `git revert`?",
                "anchors": [
                    "merge — объединение (слияние) веток: изменения одной ветки применяются к другой, например к main",
                    "revert — отмена изменений созданием НОВОГО коммита, без переписывания истории",
                    "revert применяют, когда нужно отменить ошибочный коммит",
                ],
                "keywords": ["объедин", "слиян", "отмен", "истори", "ошибочн", "новы", "ветк"],
            },
            {
                "text": "Как проверить историю и текущее состояние репозитория с помощью `git log`, `git status` и `git branch`?",
                "anchors": [
                    "git log — история коммитов",
                    "git status — состояние рабочего каталога и staging area (незакоммиченные изменения)",
                    "git branch — список веток и текущая активная ветка",
                ],
                "keywords": ["коммит", "рабоч", "каталог", "индекс", "изменен", "изменё", "активн", "звёздочк"],
            },
            {
                "text": "Зачем перед удалением ветки выполнять ее слияние с `main` и использовать `git branch -d`?",
                "anchors": [
                    "после merge изменения сохраняются в main и не теряются при удалении ветки",
                    "`git branch -d` удаляет только слитую (безопасную) ветку, защищая от потери неслитой работы",
                ],
                "keywords": ["сохраня", "потер", "безопасн", "слит", "откажет"],
            },
        ],
        "min_keywords": 2,
        "question_rules": [
            "Ответы ищите в блоке «Ответы на контрольные вопросы» в конце отчёта (звёздочки и точное оформление заголовка не важны).",
            "Если такого блока нет — ищите в конце отчёта явно нумерованный список ответов на 5 контрольных вопросов.",
            "Ответ засчитывается (correct=true) ТОЛЬКО если он находится в этом месте И по смыслу содержит указанные ключевые концепты (формулировка может отличаться).",
            "Копирование текста вопроса без ответа, односложный ответ («не знаю», «не понял», «да») или ответ без ключевых концептов — false.",
            "Ответы, написанные в других местах отчёта (не в блоке), не засчитываются.",
        ],
    },
}
